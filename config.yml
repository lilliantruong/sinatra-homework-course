title: sinatra homework
description: scu omis 120
template:
  repo: sinatra-homework-template
  name: sinatra-homework
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
